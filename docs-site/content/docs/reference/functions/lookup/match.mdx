---
title: "MATCH"
description: "MATCH: Returns the relative position of a lookup value in a one-dimensional array."
---

<FunctionPageSchema id="lookup/match" />

## Summary

`MATCH` supports exact and approximate modes and returns a 1-based position.

## Remarks
 - `match_type` defaults to `1` (approximate, ascending).
 - `match_type=0` performs exact matching and supports `*`, `?`, and `~` wildcards for text.
 - Exact matching never selects a blank candidate. A blank lookup value retains numeric-zero semantics and can select a real numeric zero, but not blank, text, or boolean candidates.
 - `match_type=1` looks for the largest value less than or equal to the lookup value.
 - `match_type=-1` looks for the smallest value greater than or equal to the lookup value.
 - Approximate modes require sorted data. MATCH detects unsorted input and returns `#N/A`, matching the same guard applied by VLOOKUP and HLOOKUP.
 - If no match is found, returns `#N/A`.

## Examples

<Tabs items={["Exact text match", "Approximate numeric match"]}>
<Tab value="Exact text match">
<FunctionSandbox
  title="Exact text match"
  formula={`=MATCH("B",A1:A3,0)`}
  grid={{"A1":"A","A2":"B","A3":"C"}}
  expected={"2"}
/>
</Tab>

<Tab value="Approximate numeric match">
<FunctionSandbox
  title="Approximate numeric match"
  formula={`=MATCH(27,A1:A4,1)`}
  grid={{"A1":"10","A2":"20","A3":"30","A4":"40"}}
  expected={"2"}
/>
</Tab>

</Tabs>

## Related functions

- [XMATCH](/docs/reference/functions/lookup/xmatch)
- [XLOOKUP](/docs/reference/functions/lookup/xlookup)
- [VLOOKUP](/docs/reference/functions/lookup/vlookup)
- [CHOOSE](/docs/reference/functions/lookup/choose)
- [LOOKUP](/docs/reference/functions/lookup/lookup)
- [CHOOSECOLS](/docs/reference/functions/lookup/choosecols)

## FAQ

### Why does MATCH with match_type 1 or -1 return #N/A on unsorted data?
Approximate modes assume ordered lookup data; this implementation treats detected unsorted inputs as no valid match and returns #N/A.

### When are wildcards interpreted in MATCH?
Wildcard patterns (*, ?, ~ escapes) are only applied in exact mode (match_type=0) for text lookup values.

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="lookup/match" />
{/* [formualizer-docgen:function-meta:end] */}
